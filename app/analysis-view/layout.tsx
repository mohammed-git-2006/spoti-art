

export default function AalysisViewLayout({children} : {children:React.ReactNode})
{
  return <html>
    <head>
      {/* <title>Hello</title> */}
    </head>
    <body>
      {children}
    </body>
  </html>
}